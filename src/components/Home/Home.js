import React from 'react';
import { container, title, slogan } from './styles.css';

const Home = () => (
  <div className={container}>
    <p className={title}>Duckr</p>
    <p className={slogan}>The Real Time, Cloud Based, modular platform.</p>
  </div>
  );

export default Home;
